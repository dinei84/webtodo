# 📐 Arquitetura da Aplicação

## Visão Geral

```
┌─────────────────────────────────────────────────────────────────┐
│                         FIREBASE CLOUD                          │
│  ┌──────────────────────┐        ┌──────────────────────┐      │
│  │   Authentication     │        │      Firestore       │      │
│  │  (Email/Password)    │        │   (NoSQL Database)   │      │
│  └──────────────────────┘        └──────────────────────┘      │
│           ↓                                    ↑                 │
│      JWT Token                          Admin SDK               │
└─────────────────────────────────────────────────────────────────┘
           ↓                                    ↑
           ↓                                    ↑
┌──────────────────────┐            ┌──────────────────────┐
│    FRONTEND          │            │      BACKEND         │
│    (React/Vite)      │  ←REST→    │  (Spring Boot)       │
│                      │            │                      │
│  ┌────────────────┐  │            │  ┌────────────────┐  │
│  │   Components   │  │            │  │   Controllers  │  │
│  │   - Login      │  │            │  │   - Task API   │  │
│  │   - Dashboard  │  │            │  │   - Health     │  │
│  │   - TodoList   │  │            │  └────────────────┘  │
│  └────────────────┘  │            │          ↓           │
│          ↓           │            │  ┌────────────────┐  │
│  ┌────────────────┐  │            │  │   Security     │  │
│  │  AuthContext   │  │            │  │   - Firebase   │  │
│  │  (State Mgmt)  │  │            │  │   - JWT Filter │  │
│  └────────────────┘  │            │  └────────────────┘  │
│          ↓           │            │          ↓           │
│  ┌────────────────┐  │            │  ┌────────────────┐  │
│  │    Services    │  │   HTTP     │  │    Services    │  │
│  │   - API        │◄─┼────────────┼─►│   - TaskSvc    │  │
│  │   - Auth       │  │  (Axios)   │  └────────────────┘  │
│  │   - Task       │  │            │          ↓           │
│  └────────────────┘  │            │  ┌────────────────┐  │
│                      │            │  │     Models     │  │
│  Port: 3000          │            │  │   - Task       │  │
│                      │            │  │   - TaskDTO    │  │
└──────────────────────┘            │  └────────────────┘  │
                                    │                      │
                                    │  Port: 8080          │
                                    └──────────────────────┘
```

## Fluxo de Autenticação

```
┌──────┐                ┌──────────┐              ┌──────────┐              ┌──────────┐
│ User │                │ Frontend │              │ Firebase │              │ Backend  │
└──┬───┘                └────┬─────┘              └────┬─────┘              └────┬─────┘
   │                         │                         │                         │
   │ 1. Login/Register       │                         │                         │
   ├────────────────────────►│                         │                         │
   │                         │ 2. signIn(email, pwd)   │                         │
   │                         ├────────────────────────►│                         │
   │                         │                         │                         │
   │                         │ 3. JWT Token            │                         │
   │                         │◄────────────────────────┤                         │
   │                         │                         │                         │
   │                         │ 4. Store token          │                         │
   │                         │ (in memory)             │                         │
   │                         │                         │                         │
   │ 5. Navigate Dashboard   │                         │                         │
   │◄────────────────────────┤                         │                         │
   │                         │                         │                         │
   │ 6. Create Task          │                         │                         │
   ├────────────────────────►│                         │                         │
   │                         │ 7. POST /api/tasks      │                         │
   │                         │    Header: Bearer {token}                         │
   │                         ├──────────────────────────────────────────────────►│
   │                         │                         │                         │
   │                         │                         │ 8. Verify Token        │
   │                         │                         │◄────────────────────────┤
   │                         │                         │                         │
   │                         │                         │ 9. Token Valid         │
   │                         │                         ├────────────────────────►│
   │                         │                         │                         │
   │                         │                         │                    10. Save
   │                         │                         │                    to Firestore
   │                         │                         │◄────────────────────────┤
   │                         │                         │                         │
   │                         │ 11. Task Created        │                         │
   │                         │◄──────────────────────────────────────────────────┤
   │                         │                         │                         │
   │ 12. Display Task        │                         │                         │
   │◄────────────────────────┤                         │                         │
   │                         │                         │                         │
```

## Camadas da Aplicação

### Frontend (React)
```
┌─────────────────────────────────────┐
│         Presentation Layer          │
│  (Components: Login, Dashboard,     │
│   TodoList, TaskItem, TaskForm)     │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│         State Management            │
│      (AuthContext, useState,        │
│           useEffect)                │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│         Business Logic              │
│  (Services: authService,            │
│   taskService, api interceptors)    │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│         External APIs               │
│  (Firebase Auth, Backend REST API)  │
└─────────────────────────────────────┘
```

### Backend (Spring Boot)
```
┌─────────────────────────────────────┐
│          REST Controllers           │
│     (TaskController, Health)        │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│         Security Layer              │
│  (FirebaseAuthenticationFilter,     │
│   SecurityConfig, UserPrincipal)    │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│         Business Logic              │
│        (TaskService)                │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│         Data Access Layer           │
│    (Firestore via Firebase SDK)     │
└─────────────────────────────────────┘
```

## Segurança - Isolamento de Dados

```
User A                         User B
  │                              │
  │ Token A                      │ Token B
  ├──────────┐                   ├──────────┐
  ↓          ↓                   ↓          ↓
┌────────────────┐         ┌────────────────┐
│ Backend Server │         │ Backend Server │
│   validates    │         │   validates    │
│   Token A      │         │   Token B      │
└────────┬───────┘         └────────┬───────┘
         │                          │
         │ uid = "user_a"           │ uid = "user_b"
         ↓                          ↓
    ┌────────────────────────────────────┐
    │          Firestore                 │
    │                                    │
    │  Tasks Collection:                 │
    │  ┌──────────────────────────┐      │
    │  │ { id: "1",               │      │
    │  │   userId: "user_a",      │ ←────┤ User A pode acessar
    │  │   title: "..." }         │      │
    │  └──────────────────────────┘      │
    │                                    │
    │  ┌──────────────────────────┐      │
    │  │ { id: "2",               │      │
    │  │   userId: "user_b",      │ ←────┤ User B pode acessar
    │  │   title: "..." }         │      │
    │  └──────────────────────────┘      │
    └────────────────────────────────────┘
```

## Tecnologias por Camada

### Frontend
- **UI Framework**: React 18
- **Build Tool**: Vite
- **Routing**: React Router DOM
- **HTTP Client**: Axios
- **Authentication**: Firebase Client SDK
- **Styling**: CSS3 (Custom)

### Backend
- **Framework**: Spring Boot 3.2.2
- **Language**: Java 17
- **Security**: Spring Security + Firebase Admin SDK
- **Database**: Firestore (NoSQL)
- **Build Tool**: Maven
- **Validation**: Jakarta Bean Validation

### Cloud Services
- **Authentication**: Firebase Authentication
- **Database**: Cloud Firestore
- **Hosting**: (pode usar Firebase Hosting, Vercel, Netlify para frontend)
- **Backend Hosting**: (pode usar Cloud Run, AWS, Heroku)

## Padrões de Projeto Utilizados

### Frontend
- **Context API**: Gerenciamento de estado de autenticação
- **Custom Hooks**: Reutilização de lógica
- **Service Layer**: Separação de lógica de negócio
- **Interceptors**: Injeção automática de tokens JWT

### Backend
- **Layered Architecture**: Controller → Service → Data Access
- **DTO Pattern**: Validação e transferência de dados
- **Filter Chain**: Validação de tokens
- **Dependency Injection**: Inversão de controle com Spring

## Principais Endpoints

| Método | Endpoint        | Auth | Descrição                |
|--------|----------------|------|--------------------------|
| GET    | /api/health    | ❌   | Health check             |
| GET    | /api/tasks     | ✅   | Listar tasks do usuário  |
| GET    | /api/tasks/:id | ✅   | Buscar task específica   |
| POST   | /api/tasks     | ✅   | Criar nova task          |
| PUT    | /api/tasks/:id | ✅   | Atualizar task           |
| DELETE | /api/tasks/:id | ✅   | Deletar task             |

## Modelo de Dados

### Task (Firestore Collection: "tasks")
```json
{
  "id": "string (auto-generated)",
  "userId": "string (Firebase UID)",
  "title": "string (1-200 chars)",
  "description": "string (max 1000 chars)",
  "completed": "boolean",
  "priority": "string (LOW|MEDIUM|HIGH)",
  "createdAt": "string (ISO DateTime)",
  "updatedAt": "string (ISO DateTime)",
  "dueDate": "string (ISO DateTime, optional)"
}
```

## Escalabilidade

### Horizontal
- ✅ Backend stateless (pode escalar horizontalmente)
- ✅ Firestore gerenciado (escala automaticamente)
- ✅ Firebase Auth gerenciado (escala automaticamente)

### Performance
- ✅ Indexação automática no Firestore
- ✅ Cache de tokens no frontend
- ✅ Lazy loading de componentes (pode ser implementado)
- ✅ Paginação de tasks (pode ser implementado)

## Segurança

### Frontend
- ✅ Tokens armazenados em memória (não em localStorage)
- ✅ HTTPS obrigatório em produção
- ✅ Validação de entrada
- ✅ Rotas protegidas

### Backend
- ✅ Validação de JWT em todas as requisições
- ✅ Isolamento de dados por usuário
- ✅ CORS configurado
- ✅ Validação de DTOs
- ✅ Logging de tentativas de acesso

### Firebase
- ✅ Service Account com permissões mínimas
- ✅ Regras de segurança no Firestore (recomendado configurar)
- ✅ Rate limiting (configurável)
