# 📊 Estrutura de Deploy - Diagrama

```
┌─────────────────────────────────────────────────────────────────┐
│                         🌐 USUÁRIO                              │
│                     (Browser/Mobile)                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ HTTPS
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                     ☁️  VERCEL (Frontend)                       │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  React 18 + Vite                                           │ │
│  │  - SPA com React Router                                    │ │
│  │  - Firebase Client SDK (Auth)                              │ │
│  │  - Axios para API calls                                    │ │
│  │  - CSS moderno com animações                               │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  Environment Variables:                                         │
│  • VITE_API_BASE_URL                                           │
│                                                                  │
│  Deploy: Git push → Auto deploy                               │
│  CDN: Global edge network                                      │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ REST API (HTTPS)
                         │ Authorization: Bearer {JWT}
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    🚀 RENDER (Backend)                          │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Spring Boot 3.2.2 (Java 17)                               │ │
│  │  - REST API endpoints                                      │ │
│  │  - Firebase Admin SDK (Token validation)                   │ │
│  │  - Spring Security                                          │ │
│  │  - CORS configuration                                       │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  Environment Variables:                                         │
│  • FIREBASE_CREDENTIALS_JSON                                   │
│  • SPRING_PROFILES_ACTIVE=production                          │
│  • ALLOWED_ORIGINS                                             │
│                                                                  │
│  Deploy: Git push → Docker build → Auto deploy                │
│  Health Check: /api/health                                     │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ Firebase Admin SDK
                         │ (Service Account)
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   🔥 FIREBASE (Google Cloud)                    │
│  ┌───────────────────┐  ┌───────────────────┐                 │
│  │  Authentication   │  │   Firestore DB    │                 │
│  │  - Email/Password │  │   - Tasks         │                 │
│  │  - JWT Tokens     │  │   - Users         │                 │
│  │  - User Management│  │   - Real-time     │                 │
│  └───────────────────┘  └───────────────────┘                 │
│                                                                  │
│  Security Rules: Firestore validation                          │
│  Region: Multi-region (auto)                                   │
└─────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                     🔄 CI/CD PIPELINE                           │
│                                                                  │
│  GitHub Repository (main branch)                               │
│         │                                                        │
│         ├──► GitHub Actions (CI)                               │
│         │    ├─ Build Backend (Maven)                          │
│         │    ├─ Run Tests                                      │
│         │    ├─ Build Frontend (npm)                           │
│         │    └─ Docker Build Test                              │
│         │                                                        │
│         ├──► Vercel (Auto Deploy Frontend)                     │
│         │    └─ Preview URLs para PRs                          │
│         │                                                        │
│         └──► Render (Auto Deploy Backend)                      │
│              └─ Health checks                                   │
│                                                                  │
│  Trigger: git push origin main                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 🔐 Fluxo de Autenticação

```
┌──────────┐                 ┌──────────┐                 ┌──────────┐
│ Frontend │                 │ Firebase │                 │ Backend  │
│ (Vercel) │                 │   Auth   │                 │ (Render) │
└────┬─────┘                 └────┬─────┘                 └────┬─────┘
     │                            │                            │
     │ 1. Login (email/password)  │                            │
     ├───────────────────────────►│                            │
     │                            │                            │
     │ 2. JWT Token               │                            │
     │◄───────────────────────────┤                            │
     │                            │                            │
     │ 3. API Request + Token     │                            │
     ├────────────────────────────────────────────────────────►│
     │                            │                            │
     │                            │ 4. Validate Token          │
     │                            │◄───────────────────────────┤
     │                            │                            │
     │                            │ 5. Token Valid             │
     │                            ├───────────────────────────►│
     │                            │                            │
     │                            │                     6. Query Firestore
     │                            │                            │
     │ 7. Response                │                            │
     │◄───────────────────────────────────────────────────────┤
     │                            │                            │
```

## 📈 Escalabilidade

### Frontend (Vercel)
- ✅ Auto-scaling via CDN
- ✅ Edge caching
- ✅ Serverless
- ✅ Global distribution

### Backend (Render)
- ✅ Auto-scaling (plano pago)
- ✅ Health monitoring
- ✅ Zero-downtime deploys
- ⚠️  Free tier: dorme após 15min inatividade

### Database (Firestore)
- ✅ Auto-scaling
- ✅ Multi-region replication
- ✅ Real-time sync
- ✅ 1GB free/day

## 💰 Custos Estimados

### Free Tier (Desenvolvimento)
```
Vercel:    100GB bandwidth/mês     → R$ 0,00
Render:    750 horas/mês           → R$ 0,00
Firebase:  1GB storage + 10GB      → R$ 0,00
           network/dia

Total:                              → R$ 0,00/mês
```

### Produção (Estimativa para ~10k users/mês)
```
Vercel:    Pro Plan                → R$ 100,00/mês
Render:    Starter (7$/mês)        → R$ 35,00/mês
Firebase:  Blaze (pay-as-you-go)   → R$ 50,00/mês

Total:                              → R$ 185,00/mês
```

## 🚦 Status e Monitoring

### Vercel Dashboard
- Real-time deployment logs
- Analytics de performance
- Error tracking
- Preview deployments

### Render Dashboard
- CPU/Memory metrics
- Request logs
- Health check status
- Deploy history

### Firebase Console
- Auth usage
- Firestore usage
- Performance monitoring
- Crash analytics

## 🔗 Links Úteis

- [Vercel Docs](https://vercel.com/docs)
- [Render Docs](https://render.com/docs)
- [Firebase Docs](https://firebase.google.com/docs)
- [Spring Boot on Render](https://render.com/docs/deploy-spring-boot)
- [Vite on Vercel](https://vercel.com/docs/frameworks/vite)
